## linksys-config-repack

A simple cli tool to unpack/repack Linksys config backup files(v2).

Tested on EA2700 and EA6200 running the latest firmwares. Afaik, the older firmware versions used a different(v1, DES-encoded) backup format.
